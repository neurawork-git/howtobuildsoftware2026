# Backlog

Rolling list of not-yet-scheduled work. Newest first. Mirror significant items
into the relevant PRD under `.claude/PRPs/prds/`.

## claudemd-lerner: enforce three base docs folders + one-liner CLAUDE.md

**Date:** 2026-07-23
**Skill:** `claudemd-lerner`
**Status:** open

The learner should **always** scaffold and maintain three base `docs/` folders:

- `docs/troubleshooting/`
- `docs/patterns/`
- `docs/rules/`

These three are exactly the categories that belong in `CLAUDE.md` (rules,
patterns, troubleshooting). Fine-tune the learner so that:

- On install / update it creates the three folders if missing.
- Every **new** `CLAUDE.md` entry lands as a **one-liner only** in `CLAUDE.md`,
  with the full explanation written to the matching `docs/` folder and **linked**
  from the one-liner.
- Goal: `CLAUDE.md` stays small; detail lives in `docs/`.

Requires tuning the `claudemd-lerner` `AGENTS.md` constitution (routing rule:
category → docs folder + one-line back-link) and the seed/update engine
(folder scaffolding).

- [x] **`$MAIN_ROOT` is used in `nw-ship-pr.md` but computed nowhere** — the Phase 4.5 fix removed the only line that resolved it (`MAIN_ROOT=$(cd "$(git rev-parse --git-common-dir)/.." && pwd)`), while eight later usages remain (Phase 6.5 config write + gitignore, 8.1 prose, the 8.4 cleanup block) and the 4.5 escape hatch still recommends it. Shell state does not survive between Bash calls, so the variable was already empty there — the consequence is a failed command, not damage. Resolve it once in the ground rules alongside the `is_main_checkout` probe, and state that the path is inserted literally.  (`plugins/neurawork-cc-harness/commands/nw-ship-pr.md`, ship-pr deferred #29)
- [x] **`claudemd-lerner/install.py` copies plugin-only `_shared` tests into the target** — its `_copy_code` runs `copytree(SHARED_SRC, …, ignore=ignore_patterns("__pycache__"))`, so `_shared/tests/test_manifest.py` and `test_version_check.py` land in every installed copy. Both assert plugin-level facts (the marketplace manifest, `<plugin>/hooks/version-check.py`) that do not exist in an install, so they fail on arrival. `compliance-compiler/install.py` already solves this with a `PLUGIN_ONLY_SHARED_TESTS` exclusion plus a prune of stale copies an older install left behind — port that to `claudemd-lerner` and `knowledge-compiler`. Surfaced by the ADOPT re-install in the rules-init work, where the two files appeared as untracked additions and were deleted by hand. **Done 2026-09-02:** PR #49 extracted `engines/_shared_install.py` with the exclusion and a prune of stale copies, and all four installers call it.  (`plugins/neurawork-cc-harness/engines/{claudemd-lerner,knowledge-compiler}/install.py`)
- [x] **`/nw-ship-pr` should capture open items, not only review findings** — Phase 6.5 persists deferred review findings, but everything else the run surfaces (unverified claims, degraded config, known-broken state named in the Phase 9 report) is only spoken in the report and lost when the session ends. Extend Phase 6.5 so the deferred-findings sink also takes the open items from the Phase 5 explanation and the Phase 9 report, and state in the command that open work is written to the backlog rather than only mentioned. Keep the existing rule that 0 items means no write and no empty commit.  (`plugins/neurawork-cc-harness/commands/nw-ship-pr.md`, ship-pr deferred #32)
- [x] **`validate_commands` is empty, so the pre-merge gate never runs** — `.claude/ship-pr.local.md` declares the key with no items, so Phase 4.5 reports SKIP on every run and the merge gate has no deterministic evidence behind it; PR #32 was merged on manually-run suites. Seed it with this repo's authoritative commands from `CLAUDE.md`: the four per-directory `unittest discover` runs and `uvx ruff check`. **Done 2026-08-21:** the key carries six commands (five per-directory engine suites plus the prompt-asset suite from the plugin root); `uvx ruff check` is deliberately excluded because a repo-root run reports 268 pre-existing findings (the config note said ~145 when it was written on 2026-08-21) and the gate would be a permanent false RED.  (`.claude/ship-pr.local.md`, ship-pr deferred #32)
- [x] **`claudemd-lerner` has never applied a daily log** — `scripts/state.json` does not exist and `scripts/last-update.json` is stamped 2026-06-25, while five logs sit in `daily/` (newest 2026-08-20). The SessionStart gate keeps spawning `update.py` detached with stdout on `DEVNULL`, so the failure is silent, and the fresh `cl-update.lock` suppresses further spawns for six hours. **Done 2026-08-27:** the cause was `update.py` importing `_shared.repo_guard` with no `sys.path` bootstrap — the only payload script in any engine missing the line — so every invocation died at import before `argparse`. Fixed in PR #45, together with the two mechanisms that hid it (the unconditional completion stamp, and the hook spawning the child with both streams at `/dev/null`) and a doctor WARN that names this state.  (`claudemd-lerner/scripts/`, ship-pr deferred #32)
- [x] **Implement the harness doctor** — a read-only `/nw-doctor` (`scripts/harness_probe.py` + `scripts/doctor.py`) covering engine discovery, version and `_shared/` drift, install integrity, hook wiring, and queue health. The hooks.json bug fixed in PR #32 means no health signal had ever fired in a user's repo, so this is the first one that works. **Done 2026-08-27:** shipped in PR #40, with PR #42 fixing the queue read to come from the main checkout when the doctor runs from a worktree.  (`.claude/PRPs/plans/harness-doctor.plan.md`, ship-pr deferred #32)
- [ ] **PR #36 was merged with its last commit unreviewed** — the review workflow ran against `652c90c`; the follow-up commit `7b6b811` was pushed afterwards and merged on the validation gate alone (GREEN, 6/6). It changes only the implementation report and one backlog line — no code, no command prose — so the exposure is a wrong sentence in a report, not a behaviour. Read it once; if it holds, close this.  (`.claude/PRPs/reports/ship-pr-open-item-capture-report.md`, ship-pr deferred #36)
- [ ] **A stale `validate_commands` transcription on another checkout runs the same suite twice** — PR #38 narrowed the key to non-test extras and moved the test commands into the `neurawork-cc-harness:rules` block, but `.claude/ship-pr.local.md` is gitignored and per-machine: only this machine's copy was emptied. On any other checkout the old six-command transcription stays in the key, and Phase 4.5 drops only **exact**-string duplicates — a transcription that differs by a space or a path would run the same suite a second time rather than being deduped. Either normalise the merged list before deduping, or have Phase 0.2 report when a `validate_commands` entry looks like a test command the block already carries.  (`plugins/neurawork-cc-harness/commands/nw-ship-pr.md`, ship-pr deferred #38)
- [ ] **`<main-root>` is derived without `--path-format=absolute`, so in the main checkout it resolves to `.`** — PR #41 replaced the unbound `$MAIN_ROOT` with a `<main-root>` placeholder resolved once in Phase 0.1, and codified the rule as "the parent of `git rev-parse --git-common-dir`". In a linked worktree that command prints an absolute path, but in the main checkout it prints the relative `.git`, whose parent is `.` — cwd-dependent, so Phase 0.2's config read and Phase 6.5's config/gitignore writes land in the wrong directory whenever the Bash cwd is not the repo root. The file already documents this exact hazard for the `is_main_checkout` probe eight lines above. Not a regression (the previous unbound variable expanded to empty and ran against `/`), but the rule is now written down and should be right. Fix: add `--path-format=absolute` to the Phase 0.1 probe, say "absolute parent" in the ground rule and the 0.1 prose, and relax `test_main_root_is_resolved_in_phase_0_before_any_use` to assert on `--git-common-dir` rather than the full command string.  (`plugins/neurawork-cc-harness/commands/nw-ship-pr.md`, ship-pr deferred #41)
- [x] **`stack-base/_shared/settings.py` is a stale hand copy with no matcher support** — every other install refreshes its `_shared/` through its own installer, but `stack-compiler` has none until PRD phase 5, so `stack-base/`'s copy still carries the pre-matcher `merge_hooks(repo_root, hooks: list[tuple[str, str, int, str]])`. Harmless today because nothing in `stack-base/` imports it, which is also why PR #41 deliberately left it alone rather than hand-copying over it. **Done 2026-08-27:** PRD phase 5 shipped `engines/stack-compiler/install.py`; the ADOPT run against this repo refreshed `stack-base/_shared/` (settings.py now byte-identical to the engine copy) and added the missing `_shared/tests/`.  (`stack-base/_shared/settings.py`, ship-pr deferred #41)
- [ ] **The `/nw-doctor` queue fix command interpolates the checkout path unquoted** — when the doctor runs from a worktree, `check_queue` builds `cd {queue_root} && {command}` so the remediation acts on the checkout it actually read. The path is interpolated bare, so a copy-pasted fix breaks on a repo path containing a space. Harmless in this repo, not in every repo the plugin installs into. Quote it (`cd "<path>" && …`); the absolute lock path in the stall ERROR has the same exposure. Raised as a non-blocking nit by the PR #42 review and deferred rather than fixed, because a further commit would have pulled a third review round over a one-line change.  (`plugins/neurawork-cc-harness/scripts/doctor.py`, ship-pr deferred #42)
- [ ] **The stack-compiler phase 5 success signal is still unverified** — the PRD's own signal for the phase is "a second repo installs `stack-compiler` from the marketplace and the `st-` gate fires there on the next PRD write". PR #43 shipped the installer and proved the install/ADOPT/migration half (temp-repo suite + this repo's own migration), but the end-to-end — install compliance-compiler + stack-compiler into a scratch repo, write `product.md`, run `/st-scope` → `/st-rank` → `/st-select`, then write a PRD naming an off-stack component and confirm the gate flags it — was not run: it needs `ANTHROPIC_API_KEY` and ~30 paid agents. Run it once before the next release.  (`plugins/neurawork-cc-harness/engines/stack-compiler/`, ship-pr deferred #43)
- [ ] **The `co-` hook still validates plans only, never PRDs** — `neurawork-cc-harness.prd.md` committed to a Phase 7 that extends `compliance-base/hooks/co-post-tooluse.py` from `.claude/PRPs/plans/*.plan.md` to `.claude/PRPs/prds/*.prd.md`, and `compliance-capabilities.prd.md:51` resolved its open question by pointing at exactly that phase ("A **PRD** is checked at capability/component level; a **plan** additionally at constraint level"). The phase was named in the PRD's prose and its parallelism note but never added as a table row and never built, so a PRD write is checked against the chosen stack (the `st-` hook) but not against the compliance catalog. Both PRDs were archived to `prds/completed/` on 2026-08-27 with this as the one carried-forward item; the `st-` hook's debounce helper is the pattern to reuse.  (`compliance-base/hooks/co-post-tooluse.py`, from `prds/completed/neurawork-cc-harness.prd.md` Phase 7)
- [x] **Self-host `VERSION` files are not pinned against their engine `VERSION`** — PR #45 bumped `engines/claudemd-lerner/VERSION` 4 → 5 but left the self-host `claudemd-lerner/VERSION` at 4, so `/nw-doctor` would have reported a false "installed 4 is behind the shipped 5" against code that is byte-identical to the payload. Caught by review and fixed in the same PR, but nothing stops it recurring: the new drift test asserts only the *bytes* of `update.py` and `cl-session-start.py`, and the other three self-hosts (knowledge 3, compliance 5, stack 2) rely on their installer alone. Add one test that walks every engine and asserts `<self-host>/VERSION` == `engines/<engine>/VERSION` whenever the self-host dir exists. **Done 2026-09-02:** `plugins/neurawork-cc-harness/tests/test_selfhost_version.py` walks the `harness_probe` registry and does exactly that, skipping an engine whose default dir is absent; proven by setting `knowledge-base/VERSION` to 99 and watching it fail. Written right after PR #50, which moved four of these files by hand.  (`plugins/neurawork-cc-harness/tests/test_selfhost_version.py`, ship-pr deferred #45)
- [ ] **The seed agent's writes have no postcondition check** — PR #47 fixed the seed prompt's path binding, but the fix is guidance to an LLM, not enforcement. `seed.py` calls `assert_in_repo_not_dotclaude(KNOWLEDGE_DIR, root)` once *before* the run and never sees what the agent actually writes, and the agent runs with `permission_mode="acceptEdits"` and `Write` allowed — so a model that ignores the prompt can still scatter files outside the knowledge root and nothing notices. The prompt test pins the instruction, not the outcome. Add a postcondition pass after `run_seed()`: diff the working tree and fail (or report) on any created path outside `KNOWLEDGE_DIR`. The same exposure applies to `compile.py`'s agent.  (`plugins/neurawork-cc-harness/engines/knowledge-compiler/payload/scripts/seed.py`, ship-pr deferred #47)
- [ ] **A repo seeded with the pre-0.7.0 prompt may hold a stray `<repo>/knowledge/`** — the bug PR #47 fixed could have sent a whole seeded wiki one directory level too high, into `<repo>/knowledge/` instead of `<repo>/<kdir>/knowledge/`. The PR neither detects nor cleans that up, and the version bump only makes an affected install pull the corrected prompt — it does not move anything already written. This repo's own self-host is unaffected (`knowledge-base/knowledge/` is correctly populated), so the exposure is other installs. Add the check to `/nw-doctor`: a `knowledge/` directory at the repo root next to a knowledge-compiler install dir is almost certainly this, and the doctor is already the read-only place that reports such states.  (`plugins/neurawork-cc-harness/scripts/doctor.py`, ship-pr deferred #47)
- [x] **The cold-start fix was never observed end to end in a real session** — verified 2026-09-02 after the #49 merge, in a throwaway git repo with a fresh `knowledge-compiler` install, no `.venv`, and an **empty `UV_CACHE_DIR`** (the reporter's condition, not just a missing venv). `hooks/session-start.py` completed in **11.28 s** — venv creation plus a 32-package download including the 91 MB `claude-agent-sdk` — and returned valid `additionalContext`; `hooks/session-end.py` completed cold with exit 0, spawned `flush.py`, and the SDK run logged `FLUSH_OK` (no daily entry, which is correct for a two-turn synthetic transcript). 11.28 s against the old 10 s SessionEnd / PreCompact budget is the defect; against 60 s it has 5x margin. The install also shipped no `test_manifest.py` / `test_version_check.py`, confirming AC1 in a real target repo.
- [ ] **The Node test file's own header prints a command that fails** — `version-check.test.js:9` documents the suite as `node --test plugins/neurawork-cc-harness/hooks/`, but Node resolves a bare directory path as a module entry point and exits 1 with `MODULE_NOT_FOUND`. The same PR adds the correct form to `CLAUDE.md:54` ("Name the file, not the directory"), so the file contradicts the guidance it shipped with and a developer following its header sees a red build for a green suite. Change the header to name `version-check.test.js`. Raised as the one non-blocking finding of the PR #19 review and deferred rather than fixed, because a further commit would have pulled a second review round over a one-line comment.  (`plugins/neurawork-cc-harness/hooks/version-check.test.js:9`, ship-pr deferred #19)
- [ ] **The portable SessionStart hook is unverified on the platform it exists to fix** — PR #19 moved the staleness nudge to `node` + the hook exec form precisely because `python3` resolves to the Microsoft Store alias on Windows and prints to stdout, which SessionStart injects as `additionalContext` (#16). The author verified on macOS 15.7.3 / Node 22.19 and states in the PR body that Windows was not tested; the exec form's behaviour there is the one gap left. Failure would degrade to silence, not to a broken session, so it did not block the merge. Confirm on a Windows install (or have the #16 reporter confirm) that the nudge fires when an install is stale and emits nothing when it is not.  (`plugins/neurawork-cc-harness/hooks/version-check.js`, ship-pr deferred #19)
- [ ] **The registry drift guard pins the marker set but not its order, and breaks on a JS reformat** — `test_version_check_registry.py` text-parses the `ENGINES` object literal out of `version-check.js` (deliberately: the guard must hold in a checkout with no Node installed) and compares markers *sorted*. Two consequences the PR #19 review surfaced as cosmetic: a stylistic reformat of the literal — single quotes, a different line break — breaks the guard's regex rather than the hook, and because the comparison is order-insensitive, a hand-written `settings.json` wiring two hooks of one engine to two different directories could resolve to a different dir in the JS than in `find_stale`. Neither affects a real install; revisit if the map grows or the file is ever run through a formatter.  (`plugins/neurawork-cc-harness/tests/test_version_check_registry.py`, ship-pr deferred #19)
- [x] **`compliance-compiler` has no payload drift guard** — `stack-compiler` pins its
  `payload/` against the `stack-base/` self-host (`test_payload_drift.py`), so a direct edit
  to either copy that is never propagated fails the suite. `compliance-compiler` had no
  counterpart, and PR #50 walked straight into the gap: its worktree-classification fix
  edited `payload/hooks/co-post-tooluse.py` and `compliance-base/hooks/co-post-tooluse.py`
  by hand, with nothing to catch a missed mirror. Named as "Not building" in that PR's plan
  and built immediately afterwards. **Done 2026-09-02:**
  `engines/compliance-compiler/tests/test_payload_drift.py` compares `scripts/`, `hooks/`
  and the flat files (lists first, then bytes); `VERSION` is left to the plugin-root
  self-host walk so one fact has one owner. Proven by appending a comment to the installed
  hook and watching it fail.  (`plugins/neurawork-cc-harness/engines/compliance-compiler/tests/test_payload_drift.py`)
- [ ] **`knowledge-compiler` and `claudemd-lerner` have no payload drift guard** — after PR #51 all four self-hosts are VERSION-pinned (`tests/test_selfhost_version.py`), and `stack-compiler` plus `compliance-compiler` additionally pin their `payload/` bytes against the self-host. The other two do not: a direct edit to `knowledge-base/scripts/` or `claudemd-lerner/hooks/` that never reaches the payload (or the reverse) still passes the suite, and the VERSION guard cannot see it because both copies carry the same number. Port the same `test_payload_drift.py` to both — the file is near-identical, only the self-host dir name and the flat-file list differ.  (`plugins/neurawork-cc-harness/engines/{knowledge-compiler,claudemd-lerner}/tests/`, ship-pr deferred #51)
- [ ] **compliance's `payload/catalog-seed/` is outside the drift comparison** — `test_payload_drift.py` compares `scripts/`, `hooks/` and the flat files, but the six prebuilt catalog files the installer seeds (`{gdpr,soc2,iso27001}.json`, `index.md`, `capabilities.{json,md}`) are not compared against anything. They are the one payload subtree that legitimately diverges from the self-host — `compliance-base/catalog/` is a live, re-extracted artifact — so a plain byte comparison is wrong here; what is needed is a check that the shipped seed still parses and carries the frameworks the installer promises. Decide which of the two it should be before adding it.  (`plugins/neurawork-cc-harness/engines/compliance-compiler/tests/test_payload_drift.py`, ship-pr deferred #51)
- [ ] **stack-compiler's VERSION is asserted twice** — its `test_payload_drift.py` kept `test_version_is_identical` while PR #51 made `tests/test_selfhost_version.py` the owner of that fact for every engine. Two implementations of one decision, which is exactly what the new walk exists to end; compliance's guard already omits it. Harmless (both assert the same thing), so it was left alone rather than touching a working test in a cleanup PR. Drop the per-engine case and let the walk own it.  (`plugins/neurawork-cc-harness/engines/stack-compiler/tests/test_payload_drift.py`, ship-pr deferred #51)
- [ ] **The self-host walk's skip branch is reasoned, not demonstrated** — `test_selfhost_version.py` skips when the checkout is not `<repo>/plugins/<plugin>`, which is the honest answer to the round-1 nit, but no test exercises that branch: every run in this repo takes the asserting path. If the layout assumption behind `REPO_ROOT = PLUGIN_ROOT.parents[1]` is ever wrong, the test would skip silently rather than fail, and nothing would say so. Either cover the branch with a temp-dir fixture or drop the skip and let a wrong layout fail loudly.  (`plugins/neurawork-cc-harness/tests/test_selfhost_version.py`, ship-pr deferred #51)
- [ ] **PR #51's commit `2307e14` bundles ten knowledge-base files with a one-line test fix** — `git add -A` swept in eight `knowledge-base/knowledge/` articles plus `index.md` rows that this repo's own `knowledge-compiler` wrote while the branch was open. Tracked compiler output, no code, and they belong in the repo — but the commit message describes only the test fix, so `git log` misstates most of that diff. Named in the PR body rather than force-pushed out of a branch that had already been reviewed. The habit to fix is the staging, not this commit: stage the files a commit is about.  (`.claude/BACKLOG.md`, ship-pr deferred #51)

- [ ] **The tracked `compliance-base/catalog/stack.json` is stale relative to what `--scaffold` writes** — re-running the self-host scaffold rewrites `generated` (still 2026-08-13) and moves `chosen_from` ahead of `applicable` in all 68 entries, because the tracked file predates that key being emitted by `scaffold()` rather than appended by `apply_selection()`. Pre-existing: the code before PR #53 produces exactly the same diff, which is how AC5 was proved. Harmless but it makes every future `--scaffold` look like it changed something. Refresh the artifact in a commit of its own, so the churn is not mistaken for a behaviour change.  (`compliance-base/catalog/stack.json`, ship-pr deferred #53)
